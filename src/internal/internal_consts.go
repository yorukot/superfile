package internal

// These are effectively consts
var pinnedDivider = directory{
	name : "",
	location : "Pinned+-*/=?",
}
var diskDivider = directory{
	name : "",
	location : "Disks+-*/=?",
}

// superfile logo + blank line + search bar
const sideBarInitialHeight = 3